# Audio Assets

Place `timer_complete.mp3` here — a short chime sound (1–2 seconds).

You can use any royalty-free chime, e.g. from freesound.org.
The file is referenced in cooking_mode_screen.dart via AssetSource('audio/timer_complete.mp3').
